# Настройка GitHub Actions для iOS

## ✅ Что уже настроено

Созданы 3 workflow файла:

1. **ios-validate.yml** - быстрая проверка структуры (Ubuntu, бесплатно)
2. **ios-build.yml** - полная сборка (macOS, требует Xcode project)
3. **ios-test.yml** - проверка файлов (macOS)

## 🚀 Как это работает

### При каждом коммите в `ios/`:

1. **ios-validate.yml** (Ubuntu) - запускается сразу:
   - ✅ Проверяет структуру папок
   - ✅ Проверяет наличие всех файлов
   - ✅ Считает Swift файлы
   - ✅ Проверяет настройки

2. **ios-build.yml** (macOS) - запускается если есть Xcode project:
   - 🔨 Пытается установить xcodegen
   - 🔨 Генерирует проект (если есть xcodegen.yml)
   - 🔨 Собирает приложение для симулятора
   - 📦 Сохраняет артефакты

## 📋 Что нужно сделать

### Для полной сборки:

**Вариант 1: Создать Xcode проект вручную**
```bash
# На Mac:
cd ios
# Откройте Xcode, создайте проект LotteryApp.xcodeproj
git add LotteryApp.xcodeproj
git commit -m "Add Xcode project"
git push
```

**Вариант 2: Использовать xcodegen (рекомендуется)**
```bash
# На Mac:
brew install xcodegen
cd ios
xcodegen generate
git add LotteryApp.xcodeproj
git commit -m "Add Xcode project (generated)"
git push
```

## 🔍 Проверка работы

После push в репозиторий:

1. Откройте GitHub → Actions
2. Увидите 3 workflow:
   - ✅ **iOS Validate** - должен пройти успешно
   - ⚠️ **iOS Build** - может показать предупреждение если нет Xcode project
   - ✅ **iOS Tests** - проверка файлов

## 📊 Статус

- ✅ **Зеленый** = все проверки прошли
- ⚠️ **Желтый** = предупреждения (например, нет Xcode project)
- ❌ **Красный** = ошибки (нужно исправить)

## 💡 Рекомендации

1. **Сначала** закоммитьте все Swift файлы
2. **Проверьте** что `ios-validate.yml` проходит
3. **Затем** создайте Xcode project (вручную или через xcodegen)
4. **После этого** `ios-build.yml` будет собирать проект

## 🎯 Текущий статус

После первого push вы увидите:
- ✅ ios-validate.yml - должен пройти (проверяет структуру)
- ⚠️ ios-build.yml - покажет предупреждение (нет Xcode project)
- ✅ ios-test.yml - должен пройти (проверяет файлы)

После добавления Xcode project все workflows будут работать полностью!



